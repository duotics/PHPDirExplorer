# =============================================================================
# PHPDirExplorer - .htaccess Configuration
# =============================================================================
# Este archivo debe colocarse en el DIRECTORIO PADRE de .explorer
# (es decir, en la raíz de tu webserver, al mismo nivel que la carpeta .explorer)
#
# Ejemplo de estructura:
#   /var/www/html/           <-- Colocar este .htaccess aquí
#   /var/www/html/.explorer/ <-- Carpeta del proyecto
#
# IMPORTANTE: Debes ajustar "/www" en las reglas según tu configuración:
#   - Si tu DocumentRoot es /var/www/html → usa "^/$" o "^/index\.php$"
#   - Si accedes via /www/ (como en XAMPP) → usa "^/www/?$" y "^/www/index\.php$"
#
# =============================================================================

# Habilita la reescritura de URL
RewriteEngine On

# Archivo índice predeterminado para todos los directorios
# (Mantiene el comportamiento normal de index.php/index.html en subdirectorios)
DirectoryIndex index.php index.html

# =============================================================================
# OPCIÓN A: Si tu webserver sirve desde la raíz (ej: http://localhost/)
# =============================================================================
# RewriteCond %{REQUEST_URI} ^/?$
# RewriteRule ^$ .explorer/index.php [L]
#
# RewriteCond %{REQUEST_URI} ^/index\.php$
# RewriteRule ^index\.php$ .explorer/index.php [L]

# =============================================================================
# OPCIÓN B: Si accedes via subdirectorio (ej: http://localhost/www/)
# Ajusta "/www" al nombre de tu subdirectorio
# =============================================================================
RewriteCond %{REQUEST_URI} ^/www/?$
RewriteRule ^$ .explorer/index.php [L]

RewriteCond %{REQUEST_URI} ^/www/index\.php$
RewriteRule ^index\.php$ .explorer/index.php [L]
